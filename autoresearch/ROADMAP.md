# ROADMAP — 分阶段展开(每阶段扩一层能力,cheat-proof 核心不变)

> 贯穿原则:**agent 在 frame 内自由研究;扩 frame(冻结层)由人/Claude 逐阶段解锁**。
> 每阶段都保留三道门(留出验证 / 横向对比生死线 / 鲁棒性门)。

## ✅ 已完成
- **P0** 建成作弊不了的评测器(接地地图 + 速度剖面能量 + 独立碰撞复核)+ LLM 闭环(skill × /loop)
- **M1** 代码级算法研究:RRT-Connect + proxy/CHOMP 前身平滑器 → 2078,三门全过
- **Phase B** 开采样器注入点 → 负结果(瓶颈是物理地板,非采样能力)
- **M2** 强制 EXPLORE 自主换算法族(CHOMP 梯度平滑器破离散地板)→ 固定点 **2046**,三门全过;
  loop 诚实判定当前动作空间榨干,把扩 frame 决定权交回人类

## ▶ S3 — 扩 frame,给真 headroom(当前阶段)
当前动作空间已到固定点 → 先扩问题,让优化有新料。
- **S3a 解冻速度**:每段速度成为**可进化组件**(`speed_profile`),由**冻结的 BEMT U形E(v)** 计价 →
  能量最优≠最短,"能量感知"第一次非退化,一个**全新优化维度**。← 先做这个
- **S3b 程序化场景生成器**:固定生成器 + train/test split → loop 可**半自主抽新场景测泛化**(出题权在冻结生成器,不在 agent,不作弊)
- **S3c 更丰富场景**:地形起伏 / 风场 / 多航点 → 真 3D 能量权衡,翻墙不再一步到位

## ▶ S4 — 优化器 → 研究员(加真研究行为)
从"优化一个指标"扩到"做科学"。
- **S4a 诊断实验模式**:agent 自己**设计并跑消融/敏感性/结构刻画实验去理解问题**(不只降 score),产出可验证的发现
- **S4b 多智能体协作**:提议者 / 质疑者(对抗验证)/ 理论家 分工(即 workflow 那套),更像研究团队

## ▶ S5 — 综合成文
- 中期报告:P0–M2 已足够(可信优化器 + 代码级研究 + 三门 + 诚实边界)
- 终期:S3–S4(非退化能量优化 + 研究员行为 + "可信 vs 开放"的边界讨论)

---
**当前执行**:S3a(解冻速度)。做法见 program.md「研究指令」。人随时可在 program.md 改序/插方向。
